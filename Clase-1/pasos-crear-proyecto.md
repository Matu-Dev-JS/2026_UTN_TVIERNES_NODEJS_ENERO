- npm init -y: Nos permite crear un proyecto de nodejs (nos crea un package.json)
- Crear el punto de entrada de nuestra app, por ejemplo main.js
- Modificar el package.json para que tenga un script de arranque ('start')
     "scripts": {
        "start": "node src/main.js"
    },
- ejecutar el script 'npm run start'